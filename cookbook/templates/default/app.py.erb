from flask import Flask
from flask.ext.sqlalchemy import SQLAlchemy

app = Flask('ShoreWheel')
app.config['SQLALCHEMY_DATABASE_URI'] = 'postgresql+psycopg2://<%= @username %>:<%= @password %>@<%= @host %>/<%= @name %>'

db = SQLAlchemy(app)
